# 🧩 Lab 5 — Static Code Analysis

## 👩‍💻 Overview
This lab demonstrates how to apply **static code analysis tools** (`pylint`, `flake8`, and `bandit`) to detect and fix code quality and security issues in a simple Python inventory management program.

We analyzed the original `inventory_system.py` and created a cleaned version, `cleaned_inventory_system.py`, after fixing at least **4 major issues**.

---

## ⚙️ Tools Used
- **Pylint** – checks coding standards and logic errors  
- **Flake8** – enforces style guide compliance (PEP8)  
- **Bandit** – detects security vulnerabilities

---

## 🧪 Steps to Reproduce Analysis

### 1️⃣ Install dependencies
```bash
pip install pylint flake8 bandit
